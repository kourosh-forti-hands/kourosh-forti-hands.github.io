---
import { siteConfig } from "../config";

const hasProjects = siteConfig.projects && siteConfig.projects.length > 0;
const hasExperience = siteConfig.experience && siteConfig.experience.length > 0;
const hasEducation = siteConfig.education && siteConfig.education.length > 0;
const currentYear = new Date().getFullYear();
---

<!-- Enhanced Dark Theme Footer -->
<footer class="relative bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 overflow-hidden">
  <!-- Animated Background Elements -->
  <div class="absolute inset-0">
    <!-- Neural Network Pattern -->
    <div class="absolute inset-0 opacity-15">
      <svg class="w-full h-full" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <linearGradient id="footerGrad1" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" style="stop-color:#667eea;stop-opacity:1" />
            <stop offset="100%" style="stop-color:#764ba2;stop-opacity:0" />
          </linearGradient>
          <linearGradient id="footerGrad2" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" style="stop-color:#f093fb;stop-opacity:1" />
            <stop offset="100%" style="stop-color:#f5576c;stop-opacity:0" />
          </linearGradient>
          <pattern id="footer-circuit" width="120" height="120" patternUnits="userSpaceOnUse">
            <circle cx="60" cy="60" r="3" fill="url(#footerGrad1)" opacity="0.6">
              <animate attributeName="r" values="3;6;3" dur="5s" repeatCount="indefinite" />
            </circle>
            <path d="M 0 60 L 120 60 M 60 0 L 60 120" stroke="url(#footerGrad2)" stroke-width="0.5" opacity="0.4"/>
            <path d="M 30 30 L 90 90 M 90 30 L 30 90" stroke="url(#footerGrad1)" stroke-width="0.3" opacity="0.3"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#footer-circuit)" />
      </svg>
    </div>

    <!-- Floating AI Elements -->
    <div class="absolute inset-0 overflow-hidden">
      <div class="absolute top-20 left-20 w-32 h-32 transform-3d">
        <div class="w-full h-full bg-gradient-to-r from-blue-400/15 to-purple-500/15 rounded-3xl rotate-45 float stagger-1 blur-2xl"></div>
      </div>
      <div class="absolute bottom-20 right-20 w-24 h-24 transform-3d">
        <div class="w-full h-full bg-gradient-to-r from-green-400/15 to-cyan-500/15 rounded-2xl float stagger-2 blur-xl"></div>
      </div>
      <div class="absolute top-1/2 right-1/4 w-40 h-40 transform-3d">
        <div class="w-full h-full bg-gradient-to-r from-pink-400/10 to-red-500/10 rounded-full rotate-12 float stagger-3 blur-3xl"></div>
      </div>
    </div>

    <!-- Code Matrix -->
    <div class="absolute inset-0 opacity-8">
      <div class="footer-matrix">
        <span style="animation-delay: 0s;">const future = await ai.evolve()</span>
        <span style="animation-delay: 4s;">while(innovating) { build(); }</span>
        <span style="animation-delay: 8s;">return excellence;</span>
        <span style="animation-delay: 2s;">if(opportunity) connect();</span>
        <span style="animation-delay: 6s;">engineer.nextLevel++;</span>
      </div>
    </div>
  </div>

  <div class="relative z-10">
    <!-- Main Footer Content -->
    <div class="max-w-7xl mx-auto px-8 sm:px-12 lg:px-24 py-16">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-16">

        <!-- Brand & Contact Section -->
        <div class="lg:col-span-4">
          <div class="scroll-animate">
            <!-- Enhanced Brand Section -->
            <div class="mb-8">
              <div class="flex items-center gap-4 mb-6 group">
                <!-- Animated Logo -->
                <div class="relative w-16 h-16">
                  <div class="absolute inset-0 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl rotate-0 group-hover:rotate-12 transition-all duration-500"></div>
                  <div class="absolute inset-0 bg-gradient-to-br from-cyan-400 to-blue-500 rounded-2xl opacity-0 group-hover:opacity-60 transition-opacity duration-500 blur-sm"></div>
                  <div class="relative w-full h-full bg-slate-900 rounded-2xl flex items-center justify-center border border-white/20 group-hover:border-cyan-400/50 transition-colors duration-300">
                    <svg class="w-8 h-8 text-cyan-400 group-hover:text-white transition-colors duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
                    </svg>
                  </div>
                </div>
                <div>
                  <h2 class="text-3xl font-bold text-white group-hover:text-cyan-400 transition-colors duration-300">
                    {siteConfig.name}
                  </h2>
                  <p class="text-xl gradient-text-tech font-semibold">
                    {siteConfig.title}
                  </p>
                </div>
              </div>

              <!-- Bio Section -->
              <div class="glass-dark rounded-2xl p-6 border border-white/10 mb-8">
                <div class="flex items-center gap-2 mb-4">
                  <div class="w-6 h-6 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-lg flex items-center justify-center">
                    <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                    </svg>
                  </div>
                  <h3 class="text-lg font-semibold text-white">Mission</h3>
                </div>
                <p class="text-white/80 leading-relaxed">
                  Building the future through innovative AI solutions and intelligent automation.
                  Transforming complex challenges into elegant, scalable systems.
                </p>
              </div>

              <!-- Status Indicator -->
              <div class="glass-dark rounded-2xl p-4 border border-white/10">
                <div class="flex items-center justify-between">
                  <div class="flex items-center gap-3">
                    <div class="w-3 h-3 bg-green-400 rounded-full animate-pulse neon-glow"></div>
                    <span class="text-sm font-mono text-white/80">status.available</span>
                  </div>
                  <div class="text-xs text-green-400 font-mono">Open for opportunities</div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Quick Links Section -->
        <div class="lg:col-span-4">
          <div class="scroll-animate" style="animation-delay: 0.2s">
            <div class="mb-8">
              <h3 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <div class="w-8 h-8 bg-gradient-to-r from-purple-500 to-pink-600 rounded-xl flex items-center justify-center">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1" />
                  </svg>
                </div>
                Quick Links
              </h3>

              <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-1 gap-3">
                <a
                  href="#about"
                  class="footer-nav-link"
                >
                  <div class="flex items-center gap-3">
                    <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-purple-600 rounded-lg flex items-center justify-center">
                      <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                      </svg>
                    </div>
                    <span class="font-medium">About Me</span>
                  </div>
                </a>

                {hasProjects && (
                  <a
                    href="#projects"
                    class="footer-nav-link"
                  >
                    <div class="flex items-center gap-3">
                      <div class="w-8 h-8 bg-gradient-to-br from-purple-500 to-pink-600 rounded-lg flex items-center justify-center">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                        </svg>
                      </div>
                      <span class="font-medium">Projects</span>
                    </div>
                  </a>
                )}

                {hasExperience && (
                  <a
                    href="#experience"
                    class="footer-nav-link"
                  >
                    <div class="flex items-center gap-3">
                      <div class="w-8 h-8 bg-gradient-to-br from-green-500 to-emerald-600 rounded-lg flex items-center justify-center">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2-2v2m8 0V6a2 2 0 012 2v6a2 2 0 01-2 2H8a2 2 0 01-2-2V8a2 2 0 012-2V6" />
                        </svg>
                      </div>
                      <span class="font-medium">Experience</span>
                    </div>
                  </a>
                )}

                {hasEducation && (
                  <a
                    href="#education"
                    class="footer-nav-link"
                  >
                    <div class="flex items-center gap-3">
                      <div class="w-8 h-8 bg-gradient-to-br from-orange-500 to-red-600 rounded-lg flex items-center justify-center">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 14l9-5-9-5-9 5 9 5z M12 14l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14z" />
                        </svg>
                      </div>
                      <span class="font-medium">Education</span>
                    </div>
                  </a>
                )}
              </div>
            </div>
          </div>
        </div>

        <!-- Contact & Social Section -->
        <div class="lg:col-span-4">
          <div class="scroll-animate" style="animation-delay: 0.4s">
            <h3 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
              <div class="w-8 h-8 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-xl flex items-center justify-center">
                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
                </svg>
              </div>
              Connect
            </h3>

            <!-- Contact CTA -->
            <div class="mb-8">
              <a
                href={`mailto:${siteConfig.social.email}`}
                class="group block glass-dark rounded-2xl p-6 border border-white/10 hover:border-cyan-400/30 transition-all duration-300 hover:scale-105 hover:shadow-xl hover:shadow-cyan-500/10"
              >
                <div class="flex items-center gap-4 mb-4">
                  <div class="w-12 h-12 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                    </svg>
                  </div>
                  <div>
                    <h4 class="text-lg font-semibold text-white group-hover:text-cyan-400 transition-colors duration-300">
                      Get In Touch
                    </h4>
                    <p class="text-white/70 font-mono text-sm">Ready to collaborate</p>
                  </div>
                </div>
                <p class="text-white/80">
                  {siteConfig.social.email}
                </p>
              </a>
            </div>

            <!-- Enhanced Social Links -->
            <div class="mb-8">
              <h4 class="text-lg font-semibold text-white mb-4 flex items-center gap-2">
                <div class="w-6 h-6 bg-gradient-to-r from-purple-500 to-pink-600 rounded-lg flex items-center justify-center">
                  <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1" />
                  </svg>
                </div>
                Social Links
              </h4>
              <div class="flex gap-4">
                {siteConfig.social?.email && (
                  <a
                    href={`mailto:${siteConfig.social.email}`}
                    aria-label="Email"
                    class="social-link-enhanced email"
                  >
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                    </svg>
                  </a>
                )}

                {siteConfig.social?.linkedin && (
                  <a
                    href={siteConfig.social.linkedin}
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label="LinkedIn"
                    class="social-link-enhanced linkedin"
                  >
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 11v5M8 8v.01M12 16v-5M16 16v-3a2 2 0 1 0 -4 0M3 7a4 4 0 0 1 4 -4h10a4 4 0 0 1 4 4v10a4 4 0 0 1 -4 4h-10a4 4 0 0 1 -4 -4z" />
                    </svg>
                  </a>
                )}

                {siteConfig.social?.github && (
                  <a
                    href={siteConfig.social.github}
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label="GitHub"
                    class="social-link-enhanced github"
                  >
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19c-4.3 1.4 -4.3 -2.5 -6 -3m12 5v-3.5c0 -1 .1 -1.4 -.5 -2c2.8 -.3 5.5 -1.4 5.5 -6a4.6 4.6 0 0 0 -1.3 -3.2a4.2 4.2 0 0 0 -.1 -3.2s-1.1 -.3 -3.5 1.3a12.3 12.3 0 0 0 -6.2 0c-2.4 -1.6 -3.5 -1.3 -3.5 -1.3a4.2 4.2 0 0 0 -.1 3.2a4.6 4.6 0 0 0 -1.3 3.2c0 4.6 2.7 5.7 5.5 6c-.6 .6 -.6 1.2 -.5 2v3.5" />
                    </svg>
                  </a>
                )}
              </div>
            </div>

            <!-- Tech Stack Mini Display -->
            <div class="glass-dark rounded-2xl p-4 border border-white/10">
              <h4 class="text-sm font-semibold text-white mb-3 flex items-center gap-2">
                <div class="w-4 h-4 bg-gradient-to-r from-green-500 to-emerald-600 rounded flex items-center justify-center">
                  <div class="w-2 h-2 bg-white rounded-full"></div>
                </div>
                Currently Working With
              </h4>
              <div class="flex flex-wrap gap-2">
                <span class="tech-badge">AI/ML</span>
                <span class="tech-badge">Python</span>
                <span class="tech-badge">AWS</span>
                <span class="tech-badge">React</span>
                <span class="tech-badge">TypeScript</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Footer Bottom -->
    <div class="border-t border-white/10 bg-slate-900/50 backdrop-blur-sm">
      <div class="max-w-7xl mx-auto px-8 sm:px-12 lg:px-24 py-8">
        <div class="flex flex-col md:flex-row items-center justify-between gap-6">

          <!-- Copyright -->
          <div class="flex items-center gap-4">
            <div class="flex items-center gap-2">
              <div class="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg flex items-center justify-center">
                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
                </svg>
              </div>
              <p class="text-white/70 font-mono">
                © {currentYear} <span class="text-cyan-400 font-semibold">{siteConfig.name}</span>
              </p>
            </div>
            <div class="hidden md:block w-px h-6 bg-white/20"></div>
            <p class="text-white/60 text-sm font-mono">
              Built with passion & precision
            </p>
          </div>

          <!-- Back to Top -->
          <a
            href="#hero"
            class="group flex items-center gap-3 glass-dark px-6 py-3 rounded-xl border border-white/10 hover:border-cyan-400/30 text-white/80 hover:text-white transition-all duration-300 hover:scale-105"
          >
            <svg class="w-5 h-5 group-hover:text-cyan-400 transition-colors duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 15l7-7 7 7" />
            </svg>
            <span class="font-medium">Back to Top</span>
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>

<script>
  // Enhanced Footer Functionality
  class EnhancedFooter {
    constructor() {
      this.init();
    }

    init() {
      this.setupSmoothScrolling();
      this.setupSocialTracking();
      this.setupAnimations();
    }

    setupSmoothScrolling() {
      const footerLinks = document.querySelectorAll('.footer-nav-link, footer a[href^="#"]');

      footerLinks.forEach(link => {
        link.addEventListener('click', (e) => {
          e.preventDefault();
          const targetId = link.getAttribute('href');
          const targetSection = document.querySelector(targetId);

          if (targetSection) {
            const headerHeight = document.querySelector('header')?.offsetHeight || 80;
            const targetPosition = targetSection.offsetTop - headerHeight - 20;

            window.scrollTo({
              top: targetPosition,
              behavior: 'smooth'
            });
          }
        });
      });
    }

    setupSocialTracking() {
      // Optional: Add analytics tracking for social link clicks
      const socialLinks = document.querySelectorAll('.social-link-enhanced');

      socialLinks.forEach(link => {
        link.addEventListener('click', (e) => {
          const platform = link.classList.contains('linkedin') ? 'LinkedIn' :
                          link.classList.contains('github') ? 'GitHub' : 'Email';

          // Analytics event could be added here
          console.log(`Social link clicked: ${platform}`);
        });
      });
    }

    setupAnimations() {
      // Intersection Observer for footer animations
      const footerElements = document.querySelectorAll('.scroll-animate');

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.style.opacity = '1';
            entry.target.style.transform = 'translateY(0)';
          }
        });
      }, {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
      });

      footerElements.forEach(element => {
        element.style.opacity = '0';
        element.style.transform = 'translateY(30px)';
        element.style.transition = 'opacity 0.8s ease-out, transform 0.8s ease-out';
        observer.observe(element);
      });
    }
  }

  // Initialize enhanced footer
  document.addEventListener('DOMContentLoaded', () => {
    new EnhancedFooter();
  });
</script>

<style>
  /* Footer Matrix Animation */
  @keyframes footerMatrixFall {
    0% {
      transform: translateY(-100vh) rotateZ(0deg);
      opacity: 0;
    }
    10% {
      opacity: 1;
    }
    90% {
      opacity: 1;
    }
    100% {
      transform: translateY(100vh) rotateZ(360deg);
      opacity: 0;
    }
  }

  .footer-matrix {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    overflow: hidden;
  }

  .footer-matrix span {
    position: absolute;
    top: -20px;
    font-family: 'IBM Plex Mono', monospace;
    font-size: 12px;
    color: #00ff41;
    animation: footerMatrixFall 15s linear infinite;
    opacity: 0.6;
    text-shadow: 0 0 5px #00ff41;
    white-space: nowrap;
  }

  .footer-matrix span:nth-child(1) { left: 5%; animation-duration: 12s; }
  .footer-matrix span:nth-child(2) { left: 25%; animation-duration: 14s; }
  .footer-matrix span:nth-child(3) { left: 45%; animation-duration: 11s; }
  .footer-matrix span:nth-child(4) { left: 65%; animation-duration: 13s; }
  .footer-matrix span:nth-child(5) { left: 85%; animation-duration: 15s; }

  /* Footer Navigation Links */
  .footer-nav-link {
    @apply block p-4 glass-dark rounded-xl border border-white/10 text-white/80 hover:text-white hover:border-cyan-400/30 hover:bg-white/5 transition-all duration-300 hover:scale-105 hover:shadow-lg;
  }

  .footer-nav-link:hover {
    box-shadow: 0 4px 15px rgba(6, 182, 212, 0.15);
  }

  /* Enhanced Social Links */
  .social-link-enhanced {
    @apply w-14 h-14 rounded-xl flex items-center justify-center transition-all duration-300 hover:scale-110 relative overflow-hidden;
  }

  .social-link-enhanced.email {
    @apply bg-gradient-to-br from-cyan-500/20 to-blue-600/20 border border-cyan-400/30 text-cyan-400 hover:from-cyan-500/30 hover:to-blue-600/30;
  }

  .social-link-enhanced.linkedin {
    @apply bg-gradient-to-br from-blue-500/20 to-blue-700/20 border border-blue-400/30 text-blue-400 hover:from-blue-500/30 hover:to-blue-700/30;
  }

  .social-link-enhanced.github {
    @apply bg-gradient-to-br from-purple-500/20 to-gray-700/20 border border-purple-400/30 text-purple-400 hover:from-purple-500/30 hover:to-gray-700/30;
  }

  .social-link-enhanced::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(45deg, transparent, rgba(255,255,255,0.1), transparent);
    transform: translateX(-100%);
    transition: transform 0.6s ease;
  }

  .social-link-enhanced:hover::before {
    transform: translateX(100%);
  }

  /* Tech Badges */
  .tech-badge {
    @apply px-3 py-1 bg-gradient-to-r from-white/10 to-white/5 border border-white/20 rounded-lg text-xs font-mono text-white/80 hover:bg-cyan-400/20 hover:border-cyan-400/40 hover:text-cyan-300 transition-all duration-300 cursor-default;
  }

  /* Glass Morphism Enhancement */
  .glass-dark {
    background: rgba(15, 23, 42, 0.8);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  /* Floating Animation Enhancement */
  .float.stagger-1 {
    animation: float 8s ease-in-out infinite;
    animation-delay: -3s;
  }

  .float.stagger-2 {
    animation: float 9s ease-in-out infinite;
    animation-delay: -5s;
  }

  .float.stagger-3 {
    animation: float 10s ease-in-out infinite;
    animation-delay: -2s;
  }

  /* Scroll Animation */
  .scroll-animate {
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .footer-matrix span {
      font-size: 10px;
    }

    .social-link-enhanced {
      @apply w-12 h-12;
    }
  }

  /* Enhanced Hover Effects */
  .glass-dark:hover {
    background: rgba(15, 23, 42, 0.9);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
  }

  /* Neon Glow Effect */
  .neon-glow {
    box-shadow: 0 0 10px currentColor,
                0 0 20px currentColor,
                0 0 30px currentColor;
  }
</style>